[pytest]
testpaths = tests scripts
python_files = test_*.py
pythonpath =
    .
    tools
    scripts
addopts = -ra
markers =
    integration: marks tests that run lineum.py as a subprocess (may be slow)
