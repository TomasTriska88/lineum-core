# P2 Retained Source-Schedule Package Recovery Brief

**Status:** active bounded repository-execution brief; no numerical extension authorized

**Version:** 0.1.0  
**Evidence cutoff:** 2026-07-31

**Parent decision report:** `research/foundations/lineum-root-programme-cross-branch-priority-review.md` version `0.1.0`, commit `08427a5870d6314117b1aeeaaa6dca9537de4abe`

## 1. Answer first

One exact repository-local step cannot currently be completed reliably through the ChatGPT GitHub connector: locating and extracting the retained P2 source-schedule executable and observer package from the very large root report, current tree, and commit history.

This brief delegates only that retrieval and baseline-replay inventory to Codex Max. ChatGPT retains all scientific interpretation, experiment design, threshold selection, result classification, and next-step authority.

## 2. Required execution mode

```text
Codex Max
```

Codex Ultra is not authorized. This is one tightly coupled repository-recovery lane, not three independent parallel lanes.

## 3. Repository and branch

```text
repository: TomasTriska88/lineum-core
branch: develop
```

Before analysis or execution, re-fetch and obey every current instruction source required by the repository, including:

- `AGENTS.md`;
- `.agent/rules.md`;
- `.clinerules`;
- `.cursorrules`;
- `.windsurfrules`;
- `docs/repository-boundaries.md`;
- `docs/LINEUM_CODEX_v1.md` and its relocation instructions;
- `.agent/continuous-research-reporting.md`;
- `.agent/workflows/scratch.md`;
- `.agent/workflows/model-tool-routing.md`;
- `research/foundations/AGENTS.md`;
- any additional nested rules that apply to files inspected or changed.

Verify the repository, branch, current HEAD, target file SHAs, and clean/dirty worktree before every write. Never bundle unrelated work.

## 4. Exact goal

Recover the exact retained package behind the completed P2 source-schedule and recovery challenge described in:

```text
research/foundations/lineum-continuous-source-cosmology-validation.md
root version: 0.4.14
root evidence cutoff: 2026-07-29
root blob SHA at review: 3fba3925553cd5596e46c02fa35d1db91523537d
```

The package must be sufficient to determine whether the original P2 baseline can be replayed exactly before any long-horizon continuation is designed.

## 5. Scientific question served

Can the completed centered-only `C0` formation-through-`t=20`, source-off-to-`t=60` lane be recovered and replayed as the same experiment, rather than reconstructed approximately from prose?

This retrieval does not test whether the remnant is stable. It only establishes whether the retained baseline package exists and reproduces its already reported output.

## 6. Known baseline coordinates to match

The recovered package should encode or reproduce all applicable coordinates below without post-hoc substitution:

```text
grid: 32 x 32
boundary: periodic
initial state: exact vacuum
model timestep: dt = 0.1
seeds: 12 paired seeds
source family of target lane: centered-only C0
formation schedule: source active through model time 20
continuation schedule: source off after model time 20
retained total horizon: model time 60
randomness indexing: common random arrays indexed by absolute time
observer: frozen v3 transport observer plus retained window and whole-movie rules
accounting: event schedule and admitted-input accounting
```

Reported target late-window values over model time `40-60`:

```text
mean defect count: 20.5232
final-frame mean defect count: 15.6667
mean resolved-core fraction: 0.974687
minimum resolved-core fraction: 0.941535
mean overlap: 0.99999892
mean orientation: 0.990655
active in every late-window frame: yes
sustained extinction in target lane: 0/12 seeds
whole-movie population pass: no
persistent object identities assigned: no
```

Relevant complete-`F0` comparison:

```text
formation-only winding extinction: 12/12 seeds
mean extinction time: 28.13
```

These values are matching coordinates, not permission to tune a reconstruction until they appear.

## 7. Required search coverage

Search systematically and report every category, including negative findings:

1. the complete root report, including embedded executable blocks, machine-readable outputs, hashes, reproduction commands, technical summaries, continuous ledger entries, and comments marking P0, P1, P2, or source-schedule sections;
2. `scripts/research/` and all current permanent research-scoped executable paths;
3. `tests/research/` and all tests that reference P1, P2, source schedules, centered-only sources, recovery, v3 observers, absolute-time random arrays, defect metrics, or the target output values;
4. current and historical report paths under `research/foundations/`;
5. Git history on all relevant branches for files later renamed, moved, consolidated, embedded, or deleted;
6. immutable commits associated with the P1/P2 completion dates and root-report changes;
7. generated outputs or JSON receipts only when versioned and explicitly permanent;
8. current `requirements.txt`, `requirements-dev.txt`, Python/toolchain metadata, and any environment lock relevant to replay.

Do not query, inspect, cite, or count `.scratch/` as repository evidence. A committed mention of a `.scratch/` path is only an `unversioned_local_reference`.

## 8. Required provenance matrix

For every candidate artifact, record:

| Field | Required content |
|---|---|
| exact path | current or historical repository path |
| immutable commit / blob SHA | exact identifier |
| role | runner, observer, test, output, embedded code, environment, or prose only |
| P2 relationship | direct, shared P1 prerequisite, later reconstruction, unrelated, or ambiguous |
| equation / update law | exact source family and active terms |
| schedule | exact event times and source states |
| seeds and random indexing | complete definition |
| observer | exact metrics and thresholds |
| accounting | event, admitted-input, neutrality, and conservation checks |
| environment | Python and dependency versions |
| replay command | exact command if available |
| expected hashes | source and output hashes if available |
| current status | recovered, partial, contradictory, renamed, deleted, prose-only, or not located |
| missing information | explicit list |

Do not merge different P1/P2 variants merely because they share a source name or observer.

## 9. Baseline replay authorization

A baseline replay is authorized only when one candidate package is complete enough to run without choosing new scientific parameters.

Before running:

1. copy the exact existing command and environment requirements into the active recovery report;
2. record source hashes, current HEAD, dependency versions, backend, precision, and execution policy;
3. state whether the run imports active Core, uses a frozen standalone model, or executes both;
4. run existing tests first;
5. preserve raw stdout/stderr, exit codes, JSON output, and failures;
6. run a second identical replay to test byte-level repeatability when the original contract requires it;
7. run the retained independent verifier if one exists;
8. compare every material baseline metric and hash at the smallest available observable level.

Do not modify scientific code merely to make the replay pass.

## 10. Classification outcomes

Use exactly one top-level outcome:

```text
A_recovered_and_replayed_exactly
B_recovered_and_replayed_with_declared_nonmaterial_environment_differences
C_recovered_but_replay_failed
D_partial_package_only
E_not_located_after_exhaustive_local_search
F_multiple_incompatible_P2_packages
```

For outcome `B`, list every difference and prove why it is nonmaterial. Do not use `B` to hide numerical mismatches.

Outcomes `C-F` do not authorize reconstruction, tuning, or a new numerical continuation.

## 11. Permanent output

Create or update only this recovery report:

```text
research/foundations/lineum-p2-retained-package-recovery.md
```

It must contain:

- the complete search inventory;
- the provenance matrix;
- all negative findings;
- located code and output hashes;
- exact replay commands;
- complete machine-readable replay output when executed;
- independent verification receipt;
- mismatch analysis;
- one bounded top-level outcome from Section 10;
- explicit limitations and reopen triggers;
- a concise statement of what ChatGPT must decide next.

The report must be standalone enough to audit the recovery decision. Essential evidence must not remain only in terminal scrollback, `.scratch/`, an uncommitted file, or a ClickUp comment.

## 12. Allowed repository changes

Allowed:

- create or update `research/foundations/lineum-p2-retained-package-recovery.md`;
- add a permanent research-scoped replay test only if it already exists conceptually in the recovered package and is required to preserve an exact replay receipt;
- make a minimal correction to an obviously broken reproduction command only when the original behavior remains unchanged and the correction is documented separately.

Not allowed without a new ChatGPT decision:

- changing `lineum_core/`;
- changing equations, source terms, observers, thresholds, schedules, seeds, boundaries, grids, timesteps, or scientific defaults;
- creating a new approximation and calling it P2;
- starting the long-horizon continuation;
- updating whitepapers or canonical claims;
- deleting or rewriting earlier reports;
- closing ClickUp tasks;
- choosing a replacement mechanism.

## 13. Stop conditions

Stop and report immediately when:

- a complete package has been recovered and its retained baseline replay and verifier have finished;
- exhaustive local history search reaches outcome `E`;
- multiple incompatible candidate packages produce outcome `F`;
- dependencies or supported environments cannot be installed without changing repository policy;
- a dirty or overlapping worktree prevents an isolated checkpoint;
- any proposed action would require scientific parameter choice or reinterpretation.

Do not continue into the long-horizon experiment.

## 14. Required checks before publication

At minimum:

- verify only authorized files changed;
- verify no `.scratch/`, cache, bytecode, credentials, generated junk, or unrelated changes are included;
- run every recovered existing P2 test applicable to the package;
- run the replay twice when feasible;
- run the independent verifier when recovered;
- inspect exact output hashes and decision metrics;
- record every failed or blocked check;
- use a coherent, reversible commit on `develop` only after the report is complete and the authorized checks pass.

## 15. Return package to ChatGPT

Return:

1. top-level outcome `A-F`;
2. commit SHA and branch;
3. exact changed-file list;
4. current and historical paths inspected;
5. exact runner, observer, test, and output paths with SHAs;
6. commands executed and exit codes;
7. complete baseline metrics and hashes;
8. independent verifier result;
9. all mismatches, missing items, failed checks, and environment blockers;
10. remaining uncommitted work;
11. explicit confirmation that no long-horizon run, scientific tuning, Core equation change, or whitepaper change occurred.

ChatGPT will independently evaluate whether the recovered package is sufficient and, only then, preregister the continuation horizon and decision criteria.

## 16. Copyable Codex execution instruction

```text
Mode: Codex Max.

Repository: TomasTriska88/lineum-core
Branch: develop

Read and obey every current repository instruction from the target branch before doing anything, including AGENTS.md, .agent/rules.md, .clinerules, .cursorrules, .windsurfrules, docs/repository-boundaries.md, docs/LINEUM_CODEX_v1.md and its relocation notice, .agent/continuous-research-reporting.md, .agent/workflows/scratch.md, .agent/workflows/model-tool-routing.md, research/foundations/AGENTS.md, and any nested rules that apply.

Execute only the bounded recovery defined in research/foundations/lineum-p2-retained-package-recovery-brief.md.

Goal: recover the exact retained P2 source-schedule executable, observer, tests, environment, outputs, and hashes behind the centered-only C0 formation-through-t=20, source-off-to-t=60 result; replay the retained baseline only when the package is complete enough to run without choosing new scientific parameters.

Create or update only research/foundations/lineum-p2-retained-package-recovery.md, plus a permanent research-scoped replay test only if it is already conceptually part of the recovered package and necessary to preserve the exact receipt.

Do not change lineum_core/, equations, source terms, observers, thresholds, schedules, seeds, boundaries, grids, timesteps, scientific defaults, whitepapers, or canonical claims. Do not synthesize missing code, tune to reported metrics, run the long-horizon continuation, select a replacement mechanism, or close ClickUp tasks.

Use one top-level outcome only: A_recovered_and_replayed_exactly, B_recovered_and_replayed_with_declared_nonmaterial_environment_differences, C_recovered_but_replay_failed, D_partial_package_only, E_not_located_after_exhaustive_local_search, or F_multiple_incompatible_P2_packages.

Preserve all negative findings. Never use .scratch/ as repository evidence. Stop when the bounded recovery outcome is reached or when a scientific choice would be required.

Commit a coherent verified checkpoint to develop only if the authorized report is complete, all applicable existing tests and replay checks have been run, and no unrelated work is included.

Return the outcome, commit SHA, changed files, paths and SHAs inspected, commands and exit codes, complete replay metrics and hashes, verifier result, all mismatches and blockers, and confirmation that no long-horizon run or scientific change occurred.
```

## 17. Prohibited interpretations

Successful recovery would establish only that the retained P2 numerical baseline can be reproduced from repository-local evidence. It would not establish long-horizon stability, a particle, identity, repair, heredity, life, quantum behaviour, cosmology, or correspondence with nature.
