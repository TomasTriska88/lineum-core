# Synthetic Vortex-Field Causal Observer

**Status:** active preregistration; execution not started  
**Version:** 0.1.0  
**Evidence cutoff:** 2026-08-03  
**Repository:** `TomasTriska88/lineum-core`  
**Target branch:** `develop`  
**Root scientific report:** `research/foundations/lineum-continuous-source-cosmology-validation.md`  
**Recovered root version:** 0.4.14  
**Root evidence cutoff:** 2026-07-29  
**Root blob SHA:** `3fba3925553cd5596e46c02fa35d1db91523537d`  
**Mandatory continuity companion:** `research/foundations/lineum-root-programme-continuity-and-impact-ledger.md`, version 0.3.0, blob `5304874451caf32313ad0e8e3c59e53958698d79`  
**Immediate predecessor:** `research/foundations/lineum-autonomous-cohesion-causal-path-discriminator.md`, version 0.4.0, blob `eea76f13fade63e3e62c32c78cc4ef437c6f665a`  
**Full lineage:** `lineum-continuous-source-cosmology-validation.md` -> `lineum-gnostic-enochic-dead-sea-scrolls-metaphor-audit.md` -> `lineum-pneuma-cohesion-observer-fixture.md` -> `lineum-pneuma-cohesion-observer-adversarial-audit.md` -> `lineum-autonomous-cohesion-causal-path-discriminator.md` -> this report  
**Operational task:** ClickUp `869echn1w — Audit Gnostic, Enochic, and Dead Sea Scrolls metaphors for Lineum`, current status `to do`, workspace `90121717552`, list `901217864718`  
**Scope:** A standalone known-answer observer transfer from latent reciprocal vortex-centre dynamics to a rendered complex scalar field. No Lineum module, retained P2 runner, current Core equation, public API, or whitepaper is modified or executed.  
**Central question:** Can a field-only vortex observer distinguish symmetry-equivalent relational restoration from a graph cut, a removed organizer, continuous global reference control, a teleported lookalike, charge scrambling, reflection, and a matched-count vortex gas?  
**Current confidence:** high that the experiment is discriminating by construction; unknown whether the frozen field detector and continuity observer will pass every positive and adversarial gate; zero evidence for a P2 object, Lineum particle, physical vortex composite, pneuma field, life, soul, entanglement, or nature.

## 1. Answer first

This checkpoint builds a training aquarium, not a replacement P2 experiment.

Six vortices with known charges form an irregular constellation. Their hidden centres can be maintained by reciprocal local links, cut into disconnected halves, left without a challenge-time organizer, controlled by a continuous global oracle, or replaced halfway through the movie by a newly generated but geometrically identical constellation. Every hidden state is rendered into a complex field. The observer receives only that field and must recover vortex charges, relational shape modulo translation and proper rotation, and continuous same-charge worldlines.

A valid observer must therefore say all of the following at once:

```text
same constellation after translation or rotation = same relational state;
mirror image = not automatically the same chiral state;
charge-scrambled constellation = not the same topological arrangement;
random matched-count vortex gas = not the same object;
local repair with intact causal links = positive known answer;
local repair after cross-link cut = negative known answer;
global oracle repair = snapshot-positive but externally powered;
teleported lookalike = snapshot-positive but continuity-negative and externally acted upon.
```

No outcome in this report can identify the retained P2 remnant or establish physical correspondence.

## 2. Why this follows from the complete programme

The root programme requires observers that reject smooth transported disorder, scalar puddles, population-only overlap, hidden numerical replay, undeclared sources, and image resemblance without object-level continuity.

Inherited constraints are:

- the retained P2 result is a source-off multi-defect population, not an assigned object;
- the exact retained P2 package must not be reconstructed approximately from prose;
- the minimum-flux observer is non-identifying because smooth disorder can pass;
- the first cohesion fixture was fooled by a continuous external target controller;
- the causal-path point fixture distinguished local links from global control, but its first absolute-orientation gate was invalid;
- the follow-up angular audit validated translation-and-rotation quotienting for the point fixture;
- exact state transplant supports software continuation, not autonomous identity;
- source and external-work accounting remain mandatory;
- no current `mu`, `kappa`, membrane, copying, life, particle, or cosmology role is promoted by an observer toy.

The current retained-P2 recovery lane remains blocked by unavailable local-history execution. This report is an independent known-answer observer audit and is prohibited from substituting for that recovery.

## 3. Evidence layers before execution

### 3.1 Current Lineum implementation

Not executed. The current Core fields and source law are outside this checkpoint.

### 3.2 Synthetic implementation

A latent six-centre mechanical system is rendered into a complex field. The observer sees only the rendered field. The latent centres are used solely as known-answer ground truth for detector localization and causal construction.

### 3.3 Interpretation

Passing would validate a field-observation protocol in this synthetic domain. It would not validate a Lineum object.

### 3.4 Hypothesis

A later Lineum-field observer may need the same combination of topology, symmetry quotienting, causal ablation, continuity, and source accounting. This remains untested.

## 4. Frozen latent constellation

There are six unit-mass centres with alternating integer charges:

```text
charges = [+1, -1, +1, -1, +1, -1]
theta_i = 2 pi i / 6
r_i = 1.70 + 0.22 cos(2 theta_i) + 0.13 sin(3 theta_i + 0.40)
R_i = [r_i cos(theta_i), r_i sin(theta_i)]
R is centred at zero
```

The graph contains all unique cyclic offset-1 and offset-2 edges.

```text
left group = nodes 0, 1, 2
right group = nodes 3, 4, 5
full graph = ring plus offset-2 chords
cut graph = full graph with every cross-group edge removed
```

Local forces are reciprocal central springs plus relative-velocity damping:

```text
F_ij = 4 (|x_j-x_i| - l_ij) (x_j-x_i) / |x_j-x_i|
F_i += F_ij
F_j -= F_ij
F_damp,i = -2 (v_i - mean(v))
```

The global adversarial controller is:

```text
F_global = -4 ((x - mean(x)) - R) - 2 (v - mean(v))
```

It has direct access to the absolute oriented reference constellation and is therefore not autonomous.

## 5. Frozen initialization and challenge

```text
seeds = 700..711
integration steps = 0.01 and 0.005
formation horizon = 4.0
challenge horizon = 6.0
sample interval for field worldlines = 0.5
bulk velocity = [0.15, -0.08]
initial affine transform = [[1.18, 0.10], [0.04, 0.84]]
initial centre noise sigma = 0.04
challenge displacement delta = [0.55, 0.18]
left group receives -delta/2
right group receives +delta/2
```

The challenge preserves the total centroid and every within-half distance. Velocities are reset to the common bulk velocity before the challenge.

## 6. Frozen field renderer

The complex scalar field is observational encoding only:

```text
phase(x) = global_phase + sum_i q_i atan2(y-y_i, x-x_i)
amplitude(x) = product_i tanh(|x-x_i| / 0.22)
psi(x) = amplitude(x) exp(i phase(x))
```

Primary grid and refinement grid:

```text
domain = [-6, 6] x [-6, 6]
primary grid = 96 x 96
refinement grid = 144 x 144
```

The renderer supplies no corrective force. It cannot repair or move the latent centres.

## 7. Frozen field-only detector

The primary detector computes wrapped phase circulation around every grid plaquette:

```text
winding = wrap(p01-p00)
        + wrap(p11-p01)
        + wrap(p10-p11)
        + wrap(p00-p10)
charge = round(winding / 2 pi)
```

Every nonzero plaquette is represented by its centre and integer charge. The detector is not given the latent centre positions, node labels, reference ordering, graph, intervention type, or target pose.

Ground truth is used only to audit localization error after detection.

## 8. Frozen observer components

### 8.1 Topology

Every dynamic frame must contain exactly six detected defects with charge multiset:

```text
[-1, -1, -1, +1, +1, +1]
```

### 8.2 Relational geometry

Detected centres are matched only within equal-charge classes. All charge-preserving permutations are considered. Translation is removed and the best proper rotation with determinant `+1` is fitted. Reflection is not admitted.

The normalized RMS residual is:

```text
E_geometry = RMS(aligned_source - centred_target)
             / RMS(centred_target)
```

Challenge recovery is:

```text
R_geometry = 1 - E_final / E_initial
```

### 8.3 Continuous same-charge worldlines

Consecutive detected frames are matched within charge classes by minimum total displacement. A worldline survives only when every assigned jump is at most `0.55` model-length units. The final survival fraction is the fraction of terminal detections continuously connected to the first challenged frame.

This threshold is intentionally much larger than detector quantization and ordinary sampled motion, but much smaller than the frozen teleport shift.

### 8.4 External action

Continuous global-controller action is integrated as:

```text
A_ext = integral sum_i |F_external,i| dt
```

The regenerated lookalike records the sum of instantaneous replacement displacements. Local reciprocal lanes and removed-force lanes have declared external action exactly zero.

## 9. Frozen dynamic cases

```text
L_INTACT:
    local formation;
    full reciprocal graph during challenge.

L_CUT:
    local formation;
    all cross-group links removed during challenge.

L_REMOVED:
    local formation;
    zero challenge force.

G_CONTINUOUS:
    global formation;
    global reference controller active during challenge.

G_CUT:
    global formation;
    same global controller;
    graph cut is irrelevant to its law.

G_REMOVED:
    global formation;
    zero challenge force.

REGENERATED:
    local formation;
    zero challenge force;
    halfway through the challenge the complete challenged constellation is
    replaced by the contemporaneous twin plus rigid shift [1.20, -0.80].
```

`REGENERATED` is designed to end with an excellent symmetry-quotiented snapshot while breaking every continuous worldline and registering a large external replacement action.

## 10. Frozen static adversarial controls

```text
translation-plus-rotation:
    rotate the complete reference constellation by 37 degrees and shift it
    by [0.70, -0.40]; relational error should remain small.

global phase:
    add 1.2345 radians to the complete complex field; detected defects should
    be exactly unchanged.

reflection:
    mirror the constellation across the x coordinate; proper-rotation matching
    must not silently erase chirality.

charge scramble:
    keep the centre positions but cyclically shift the charge assignment;
    geometry without topology would be fooled, but the charge-aware observer
    must reject it.

vortex gas:
    generate twelve independently seeded irregular matched-count constellations
    with the same charge multiset and comparable radial extent but no reference
    relational organization.
```

## 11. Frozen confirmatory gates

No threshold, seed, graph, force, intervention, horizon, renderer, detector, matching rule, or grid may change after this version is committed.

### 11.1 Field and static controls

```text
all dynamic frames detect exactly six defects with the frozen charge multiset;
maximum detector-to-latent localization error < 0.18;
translation-plus-rotation invariant error < 0.03 on both grids;
global-phase detections exactly equal on both grids;
reflection invariant error > 0.08 on both grids;
charge-scramble invariant error > 0.08 on both grids;
minimum vortex-gas invariant error > 0.12 on both grids.
```

### 11.2 Dynamic known answers

```text
L_INTACT:       R_geometry > 0.85 in every seed and timestep;
L_CUT:          R_geometry < 0.20 in every seed and timestep;
L_REMOVED:      R_geometry < 0.20 in every seed and timestep;
G_CONTINUOUS:   R_geometry > 0.95 and A_ext > 0.01 in every run;
G_CUT:          R_geometry > 0.95 and A_ext > 0.01 in every run;
G_REMOVED:      R_geometry < 0.20 in every run;
local cut sensitivity > 0.65 in every paired run;
global cut difference < 0.02 in every paired run.
```

### 11.3 Identity and source controls

```text
track survival > 0.95 for L_INTACT, G_CONTINUOUS, and G_CUT;
REGENERATED final snapshot error < 0.03 in every run;
REGENERATED track survival < 0.50 in every run;
REGENERATED external action > 1.0 in every run;
external action is exactly zero for L_INTACT, L_CUT, L_REMOVED, and G_REMOVED.
```

### 11.4 Independent checks

```text
explicit-loop and vectorized plaquette detectors agree exactly on base and
translation-plus-rotation controls at both grid resolutions;
explicit-loop and vectorized local-force formation agree below 1e-10;
per-seed L_INTACT recovery difference between dt=0.01 and dt=0.005 < 0.05;
mean timestep recovery difference < 0.03;
primary-versus-refined-grid final geometry-error difference < 0.03.
```

## 12. Frozen outcome classes

```text
PASS_KNOWN_ANSWER_FIELD_OBSERVER:
    every frozen gate passes.

FAIL_FIELD_DETECTION:
    topology count, charge, localization, phase, implementation, or resolution
    gates fail.

FAIL_RELATIONAL_DISCRIMINATION:
    symmetry positives fail or reflection, charge scramble, or gas passes.

FAIL_CAUSAL_DISCRIMINATION:
    local cut, removed-force, or global-controller distinctions fail.

FAIL_IDENTITY_CONTINUITY:
    regenerated lookalike is accepted as continuous identity or genuine continuous
    lanes lose their worldlines.

FAIL_NUMERICAL_STABILITY:
    timestep or independent implementation gates fail.
```

A failure is a verified negative result and triggers the owner failure gate before selecting a replacement observer. No post-hoc tuning is allowed.

## 13. Exact executable freeze

The complete standalone Python source has been frozen before execution with SHA-256:

```text
72bb13bc96da63f6a6c393903bf57443814dbb2e2240eb43737230811f6c3d1b
```

It uses only NumPy and SciPy, does not import Lineum, and implements exactly Sections 4-11. The full source and complete machine-readable output must be embedded in this report immediately after execution and before any result informs another lane.

Current execution state:

```text
execution = not_started
result = not_available
threshold_tuning = prohibited
P2_application = prohibited
```

## 14. Root-programme impact matrix before execution

| Root branch | Relation | Pre-execution impact |
|---|---|---|
| P2 retained package recovery | `unaffected` | This report cannot replace or approximate the retained package. |
| P2 vortex-gas remnant | `depends_on` and `constrains` | A future application requires exact P2 recovery and a validated field observer. |
| Minimum-flux observer limitation | `supports` | Multiple adversarial controls are required instead of one transport score. |
| Source accounting | `supports` | Global and regenerative action are recorded separately from shape recovery. |
| Causal-path point fixture | `depends_on` | The field audit transfers its graph-cut logic while hiding latent centres from the observer. |
| Identity and transplant programme | `supports` and `constrains` | Snapshot equivalence is separated from continuous history. |
| `mu x kappa`, membrane, copying, heredity | `unaffected` | No current field role or copying mechanism is tested. |
| Physical particle, life, soul, entanglement, or cosmology | `unaffected` | No physical correspondence can follow from this synthetic audit. |
| Cross-repository variants | `not_yet_compared` | No private product or OEA mechanism is imported. |

## 15. Stop conditions

Stop immediately and preserve the result when any frozen gate fails. Do not tune the detector, worldline threshold, challenge, graph, field core, or geometry thresholds in the same confirmatory version.

If all gates pass, record only a validated known-answer field-observer fixture. Do not apply it to P2, modify Core, or update a whitepaper in the same checkpoint.
