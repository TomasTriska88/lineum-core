# Lineum-Native Galactic Radial-Locking: Lane B Drift-Off Receipt

**Status:** active  
**Version:** 0.1.0  
**Evidence cutoff:** 2026-08-04  
**Scope:** causal drift-off intervention for the preregistered Lineum-native radial-locking test  
**Current confidence:** high that the current default `phi`-gradient drift is not materially responsible for the Lane A outer trace within this tested regime  
**Operational task:** ClickUp task `869edcdkk`

## 1. Lineage and boundary

Root programme: `research/lineum-native-field-stress-tests.md`, version 0.1.0.  
Parent protocol: `research/lineum-native-galactic-radial-locking-test.md`, version 0.2.0.  
Baseline receipt: `research/lineum-native-galactic-radial-locking-lane-a.md`, version 0.1.0.

This report is standalone for Lane B reproduction. It contains no external private theory, manuscript, equation, or unpublished dataset.

## 2. Plain-language result

Turning off the implemented `phi`-gradient drift changed the result by an almost negligible amount.

The weak outer trace seen in Lane A therefore was not materially generated by the current drift feedback. Under this frozen regime, it was dominated by the remaining path: initial `psi` profile, diffusion, interaction, mode coupling into `phi`, and `phi` diffusion.

The radial-locking candidate still failed the same three shape gates.

## 3. Frozen intervention

Everything from Lane A remained unchanged except:

```text
Lane A: drift_strength = -0.004
Lane B: drift_strength = 0.0
```

Frozen engine blob SHA:

`bb877021810691223a0eb960a45493a2e351112a`

Execution environment:

```text
backend = NumPy CPU
dtype = float64 / complex128
grid = 128 x 128
periodic roll boundaries = enabled
random input = none
steps = 2000
retained samples = every 10 steps from 1600 through 2000
```

As in Lane A, the environment could not clone and directly import the repository package. The run used the documented lane-specific extraction of the current NumPy path. Two fresh-process constructions agreed exactly on the reported metrics.

## 4. Lane B result

| Metric | Required | Lane A | Lane B | Lane B result |
|---|---:|---:|---:|---|
| `plateau_cv` | `<= 0.10` | `1.17279504274407` | `1.17279493302875` | fail |
| `abs(plateau_slope)` | `<= 0.15` | `3.36298790808512` | `3.3629877119416` | fail |
| `gradient_log_slope` | `[-1.25, -0.75]` | `-20.4566513290255` | `-20.4566503876771` | fail |
| `temporal_cv` | `<= 0.10` | `0.0459401725708739` | `0.0459401455106348` | pass |
| `radial_signal_ratio` | `>= 10` | `4687014.26630595` | `4687011.47095736` | pass |
| numerical stability | finite | pass | pass | pass |

Causal comparison:

```text
relative L2 difference of outer v_phi = 3.2606280777782343e-07
relative difference of outer mean v_phi = -2.6318238572540863e-07
relative L2 difference of phi_bar = 9.643814746242067e-07
maximum absolute outer v_phi difference = 1.1274825100241956e-10
```

The outer proxy changed by roughly `0.263182` parts per million.

## 5. What was actually observed

1. Lane B remained finite and avoided all caps and fail-safes.
2. Lane B failed the same shape gates as Lane A.
3. The complete radial profiles from Lane A and Lane B were nearly indistinguishable.
4. Removing drift did not remove the non-zero outer trace.
5. The implemented default drift therefore contributed negligibly to this result within the declared domain.

## 6. Mechanistic interpretation

Supported within the tested regime:

> The weak Lane A outer response is not evidence of a current Lineum gradient-feedback locking mechanism.

The trace is observationally consistent with passive evolution through diffusion and `psi -> phi` mode-coupling history.

Not established:

- that drift can never matter in another parameter or equation regime;
- that mode coupling alone is the unique explanation until Lane C is completed;
- that `mu` cannot create a distinct effect;
- that a different Lineum observer would give the same conclusion;
- any physical connection to gravity or dark matter.

## 7. Verified negative-result gate

The intended causal story for Lane A was that current `phi`-gradient feedback might contribute to a long-range locking profile. Lane B falsifies that explanation within this domain because removing the feedback leaves the profile effectively unchanged.

What failed:

- causal relevance of default drift to the measured outer trace;
- the preregistered plateau condition in both baseline and drift-off lanes.

What remained positive:

- stable non-zero outer `phi` structure;
- late-time temporal steadiness;
- a clean intervention with extremely small A/B difference.

Failure location:

- the current default drift mechanism is non-identifying here;
- the remaining trace must be assigned to another implemented path or to the observer/numerical geometry.

Qualitatively distinct repair or explanation classes:

1. `mu` structural memory creates a genuinely different long-range channel;
2. another historical Lineum equation family supplies wave, saturation, or higher-order propagation absent here;
3. the physically relevant Lineum observable is not `|d phi/dr|`;
4. the entire trace is passive diffusion geometry and should not be repaired into a positive result;
5. scale or boundary semantics suppress an otherwise active feedback regime.

No class is selected or tuned in this report.

## 8. Reproduction receipts

Compact Lane B runner SHA-256:

`180ef12b9bed4ea2792516d11d06b233d0d153ee929a4ee81ca0cd4cd00abcfd`

Compact Lane B output SHA-256:

`cadb832e998cdc6912448a0ff32c2472de1691eba8629d3ea49e17fd135f36ef`

The full extracted implementation and the independently assembled compact Lane B script agreed on every reported metric.

## 9. Executable reproduction code

```python
import json, time
import numpy as np

R=np.array([0.32,0.64,0.96,1.28,1.61,1.93,2.24,2.57,2.89,3.21,3.54,3.85,4.17,4.50,4.82,5.15,5.46,5.78,6.10,6.43,6.74,7.06,8.04,9.04,10.04,11.04,12.05,14.05,16.07,18.13,20.05,22.12,24.03,26.10,28.16,30.08,32.14,34.06,36.12,38.19,40.10,42.17,44.08],float)
SB=np.array([1084.92,590.57,410.97,329.34,268.62,247.67,227.56,205.02,200.20,208.58,208.47,196.07,179.96,164.19,150.99,138.08,126.00,113.63,101.19,86.52,70.23,57.67,40.74,31.83,26.64,21.02,15.42,6.42,2.95,2.39,1.44,0.72,0.28,0.16,0.08,0.04,0.02,0.01,0.01,0.00,0.00,0.00,0.00],float)

N=128; DT=1.; PD=.05; FD=.05; DRIFT=0.; COUPLING=.001
yy,xx=np.indices((N,N),dtype=float); c=(N-1)/2; rr=np.hypot(xx-c,yy-c)
rk=rr*(R[-1]/44.0)
psi=(.25*np.interp(rk,R,SB,left=SB[0],right=0.)/SB.max()).astype(complex)
phi=np.zeros((N,N)); k=np.ones((N,N)); mu=np.zeros((N,N))

def lap(f):
    return PD*(np.roll(f,1,0)+np.roll(f,-1,0)+np.roll(f,1,1)+np.roll(f,-1,1)-4*f)

def phi_lap(f):
    return .05*(np.roll(f,1,0)+np.roll(f,-1,0)+np.roll(f,1,1)+np.roll(f,-1,1)-4*f)

def step(psi,phi):
    dm=1.+mu
    pint=np.clip(phi,0.,10.)
    interaction=.1*np.tanh((.04*pint*k*dm)/.1)*psi
    interaction/=1.+np.abs(interaction)/10.
    gx,gy=np.gradient(phi)
    flow=DRIFT*(gx+1j*gy)*k*dm
    flow/=1.+np.abs(flow)/10.
    psi=psi+flow*DT
    psi=psi+interaction*DT
    psi=psi-.005*psi*DT
    psi=psi+lap(psi)*k*DT
    e=np.abs(psi)**2
    de=COUPLING*e*k*DT
    phi=phi+de
    psi=(psi/(np.sqrt(e)+1e-12))*np.sqrt(np.maximum(e-de,0.))
    phi=phi+k*FD*phi_lap(phi) # legacy per-update semantics
    phi=np.clip(phi,0.,1e6)
    return psi,phi

bins=np.floor(rr).astype(int)
def radial(a):
    s=np.bincount(bins.ravel(),weights=a.ravel(),minlength=57)
    n=np.bincount(bins.ravel(),minlength=57)
    return np.divide(s[:57],n[:57],out=np.zeros(57),where=n[:57]>0)
def observe(phi):
    p=radial(phi); r=np.arange(57.,dtype=float)
    g=np.abs(np.gradient(p)); v=np.sqrt(np.maximum(r*g,0.))
    b=(r>=24)&(r<=40); f=(r>48)&(r<=56)
    x=(r[b]-24.)/16.; y=v[b]/(v[b].mean()+1e-300)
    pos=(r>0)&(g>0); ls=np.full(57,np.nan)
    ls[pos]=np.gradient(np.log(g[pos]),np.log(r[pos]))
    return p,g,v,{
      "plateau_cv":float(v[b].std()/(v[b].mean()+1e-300)),
      "plateau_slope":float(np.polyfit(x,y,1)[0]),
      "gradient_log_slope":float(np.nanmedian(ls[b])),
      "outer_g_mean":float(g[b].mean()),
      "far_g_mean":float(g[f].mean()),
      "radial_signal_ratio":float(g[b].mean()/max(g[f].mean(),np.finfo(float).tiny)),
      "outer_v_mean":float(v[b].mean())}

samples=[]; maxpsi=maxphi=0.; t=time.perf_counter()
for i in range(1,2001):
    psi,phi=step(psi,phi)
    maxpsi=max(maxpsi,float(np.abs(psi).max()))
    maxphi=max(maxphi,float(phi.max()))
    if i>=1600 and i%10==0: samples.append((i,observe(phi)[3]["outer_v_mean"]))
p,g,v,m=observe(phi)
tv=np.array([x[1] for x in samples])
m["temporal_cv"]=float(tv.std()/(tv.mean()+1e-300))
criteria={
 "plateau_cv":m["plateau_cv"]<=.10,
 "plateau_slope":abs(m["plateau_slope"])<=.15,
 "gradient_log_slope":-1.25<=m["gradient_log_slope"]<=-.75,
 "temporal_cv":m["temporal_cv"]<=.10,
 "radial_signal_ratio":m["radial_signal_ratio"]>=10.,
 "numerical_stability":bool(np.isfinite(psi).all() and np.isfinite(phi).all() and np.abs(psi).max()<.99e6)}
out={"lane":"B","frozen_math_blob_sha":"bb877021810691223a0eb960a45493a2e351112a","grid":128,"steps":2000,"retention":"every 10 steps from 1600 through 2000 inclusive","runtime_seconds":time.perf_counter()-t,"max_abs_psi":maxpsi,"max_phi":maxphi,"max_mu":0.,"metrics":m,"criteria":criteria,"candidate_signal_all_criteria":all(criteria.values()),"profiles":{"r":list(range(57)),"phi_bar":p.tolist(),"g_phi":g.tolist(),"v_phi":v.tolist()},"temporal_samples":[{"step":i,"outer_v_mean":x} for i,x in samples]}
print(json.dumps(out,indent=2,sort_keys=True))
```

## 10. Machine-readable result

```json
{
  "candidate_signal_all_criteria": false,
  "change_from_lane_a": "drift_strength -0.004 -> 0.0 only",
  "comparison_to_lane_a": {
    "max_abs_phi_bar_diff": 9.926182643307975e-11,
    "max_abs_v_phi_diff_outer": 1.1274825100241956e-10,
    "max_phi_relative_difference": -4.992411023681347e-05,
    "outer_v_mean_relative_difference": -2.631823857254086e-07,
    "relative_l2_phi_bar_diff": 9.643814746242067e-07,
    "relative_l2_v_phi_diff_outer": 3.2606280777782343e-07
  },
  "criteria": {
    "gradient_log_slope": false,
    "numerical_stability": true,
    "plateau_cv": false,
    "plateau_slope": false,
    "radial_signal_ratio": true,
    "temporal_cv": true
  },
  "frozen_math_blob_sha": "bb877021810691223a0eb960a45493a2e351112a",
  "grid": 128,
  "lane": "B",
  "metrics": {
    "far_g_mean": 1.0128199741386662e-16,
    "gradient_log_slope": -20.456650387677087,
    "outer_g_mean": 4.747098836802661e-10,
    "outer_v_mean": 7.105977751398895e-05,
    "plateau_cv": 1.1727949330287541,
    "plateau_slope": -3.3629877119415994,
    "radial_signal_ratio": 4687011.4709573565,
    "temporal_cv": 0.0459401455106348
  },
  "steps": 2000
}
```

The executable output also regenerates full radial and temporal profiles.

## 11. Root-programme impact

| Root question | Lane B impact |
|---|---|
| Q1 — Galactic radial locking | contradicts default-drift causal locking in this regime |
| Q2 — Bounded saturation and attraction | finite execution only; no attractor evidence |
| Q3 — Scalar minimum and information retention | unaffected |
| Compute question | no external benchmark |

## 12. Next discriminator before replacement selection

The preregistered mechanical control is Lane C with `mode_coupling_strength = 0`. It can determine whether the remaining non-zero `phi` trace is fully dependent on the implemented `psi -> phi` channel and can detect observer or runner contamination.

However, Lane B is a verified decision-relevant negative result for the proposed drift mechanism. Repository governance requires the project owner's plain-language interpretation before a replacement mechanism is selected or tuned.
