{
    "name": "lineum-core-workspace",
    "private": true,
    "description": "Public Lineum Core research workspace",
    "scripts": {
        "build": "npm --prefix lab run build",
        "test": "npm --prefix lab run test",
        "test:e2e": "npm --prefix lab run test:e2e",
        "clean:scratch": "npx -y rimraf .scratch lab/.scratch"
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "devDependencies": {
        "vitest": "^4.0.18"
    },
    "dependencies": {
        "playwright": "^1.58.2"
    }
}
