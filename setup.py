from setuptools import setup, find_packages

setup(
    name="lineum-core",
    version="1.1.6",
    description="Lineum Core library",
    packages=find_packages(),
    install_requires=[
        # Core requirements are managed in requirements.txt or individually per project
    ],
)
